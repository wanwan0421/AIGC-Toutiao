# 实施方案

## 一、技术栈：

- **前端：**Next.js + React 18 + TypeScript
- **后端：**NestJS
- **数据库：**PostgreSQL + Redis
- **AI：**火山方舟

## 二、前端：

- **登录注册界面：**
  - 支持用户通过手机号/邮箱等方式进行登录注册
  - 支持用户安全退出登录
- **创作者工作台：**
  - 进入AI内容创作入口
  - 管理已经发布的作品、草稿箱等
  - 作品状态流转：
    1. 草稿 → 待审核 → 审核通过 → 已发布 → 已更新 / 已下线
    2. 草稿 → 待审核 → 审核驳回 → 合规改写 → 再次提交审核
- **AI内容创作编辑器：**
  - 标题、正文编辑等功能
  - 上传图片、视频、参考资料等素材
  - 输入主题、添加位置、作者声明、标签、风格等需求
  - 自动保存、发布
  - 选择或填写prompt，AI自动生成，点击生成标题、正文、配图建议等内容，用户也可以继续人工编辑
  - 发布前进行AI审核和质量评分流程
- **内容详情页：**
  - 读者查看图文内容、作者、发布时间、热度数据
- **榜单页：**
  - 实时热点/爆文/推荐
  - 无限滚动、多维排序

## 三、后端：

- **用户系统：**
  - 完成注册、登录、退出、鉴权、会话管理
- **内容系统：**
  - 文章创建、编辑、发布、下线、状态流转
- **草稿与版本：**
  - 30秒自动保存
  - 草稿恢复
  - 版本记录
  - 冲突处理
- **AI调用编排：**
  - 生成内容
  - 审核内容
  - 质量评分
  - 合规改写
- **Prompt 与素材管理：**
  - Prompt 模板的创建、分类、版本管理、启停状态
  - 素材上传、元信息记录、基础合规校验
  - 素材与文章、草稿的关联
- **榜单系统：**
  - 根据质量分、阅读热度、发布时间等计算排序
- **数据统计**
- **性能优化**
- **审核系统**

## 四、数据库：

用PostgreSQL，存储强结构、强一致、需要关联查询的数据：

1. **用户表：**用户 ID、邮箱、手机号、密码哈希、头像等。
2. **内容表：**文章标题、正文、状态、作者、发布时间、更新时间，阅读量、点赞数、收藏数、点击数等定时从Redis存回PostgreSQL。
3. **草稿表：**草稿内容、所属用户、自动保存时间。
4. **版本表：**文章版本号、历史内容、回滚信息。
5. **Prompt 表：**名称、场景、模板内容、变量定义、版本号、模型参数、状态、创建者、使用次数。
6. **素材元信息表：**文件名、类型、URL、上传者、审核状态。
7. **审核记录表：**风险类型、风险等级、审核结论、AI 返回结果。
8. **质量评分表：**总分、维度分、评分理由。
9. **用户行为表：**阅读、点赞、收藏、曝光、点击等行为事件。
10. **用户偏好表：**常用风格、创作领域、默认平台、禁用词等。

用Redis作为高性能缓存与实时数据层，通过定时同步或事件触发方式将 Redis 中的实时数据存回PostgreSQL存：

1. 登录 Session / Token 黑名单。
2. 热点榜单缓存。
3. 阅读量、点赞数、实时计数器。
4. 接口限流数据。
5. 自动保存的临时草稿缓存。
6. 排行榜ZSet，例如按热度分排序。
7. AI 请求队列或任务状态缓存。

## 五、AI能力

AI 能力由 NestJS 后端统一封装调用火山方舟 API，前端不直接调用模型接口，避免泄露 API Key

- **内容生成：**根据主题、素材、风格生成标题、正文、标签、配图建议或直接生成目标内容

- **内容审核：**识别涉黄、涉赌、涉毒、敏感信息、低俗内容等风险
- **质量评分：**从结构完整度、表达清晰度、信息价值、吸引力、合规性等维度打分
- **合规改写：**对违规或低质量内容生成替代表达
- **调用记录：**保存每次 AI 调用的输入摘要、输出结果、模型名称、耗时和错误信息

## 六、核心业务流程

1. 用户未登录进入注册
2. 用户登录后进入创作者工作台
3. 用户创建内容，填写标题、正文等内容，也可选AI提示词后端调用AI生成标题、正文、配图建议
4. 上传素材，填写主题、风格、标签等需求
5. 用户在编辑器中人工修改，系统每 30 秒自动保存草稿
6. 发布前进入AI审核与质量评分
7. 审核通过后发布；审核失败则展示风险原因，并支持一键合规改写。
8. 已发布内容进入榜单候选池
9. 榜单根据质量分、阅读热度、发布时间等综合排序
10. 读者浏览榜单和详情页，产生阅读、点赞、收藏等行为数据
11. 行为数据反哺榜单排序